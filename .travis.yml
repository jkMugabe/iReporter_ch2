language: python
python:
  - 3.6

install:
  - pip3 install -r requirements.txt
  - pip install python-coveralls
  - pip install coverage

script:
  - python3 -m pytest --cov

after_success:
  - coveralls
